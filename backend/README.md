# DiaConnect Family - Backend

FastAPI backend for diabetes management.

## Setup

1. `python -m venv venv`
2. `venv\Scripts\Activate.ps1`
3. `pip install -r requirements.txt`
4. `uvicorn app.main:app --reload`
